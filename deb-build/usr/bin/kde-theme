#!/bin/bash

# kde-theme: KDE Plasma theme + layout backup/restore utility
# Features:
# - Backup theme, layout, Kvantum, Latte, icons, wallpapers, sounds, cursors (user-level)
# - Restore theme only, layout only, or both
# - Warn about missing plasmoids
# - Apply saved global theme
# - Cross-user / cross-machine restore: rewrites old $HOME paths to current $HOME

BASE_DIR="$HOME/kde-theme-backups"

ensure_base_dir() {
    mkdir -p "$BASE_DIR"
}

# ---------- Helper: detect missing plasmoids from backup ----------
check_missing_plasmoids() {
    local backup_dir="$1"
    local cfg="$backup_dir/plasma-org.kde.plasma.desktop-appletsrc"

    if [ ! -f "$cfg" ]; then
        return
    fi

    echo "🔍 Scanning for missing plasmoids in layout…"

    mapfile -t plugins < <(grep -oP '(?<=plugin=)[^\n]+' "$cfg" 2>/dev/null | sort -u)

    declare -A installed
    for dir in /usr/share/plasma/plasmoids "$HOME/.local/share/plasma/plasmoids"; do
        if [ -d "$dir" ]; then
            for p in "$dir"/*; do
                [ -d "$p" ] || continue
                installed["$(basename "$p")"]=1
            done
        fi
    done

    local missing=()
    for plugin in "${plugins[@]}"; do
        if [ -z "${installed[$plugin]}" ]; then
            missing+=("$plugin")
        fi
    done

    if [ "${#missing[@]}" -gt 0 ]; then
        echo "⚠️  The following plasmoids from the backup layout are not installed on this system:"
        for m in "${missing[@]}"; do
            echo "   • $m"
        done
        echo "You may want to install these widgets from Discover/store.kde.org for a perfect restore."
    else
        echo "✅ All plasmoids referenced in the layout appear to be installed."
    fi
}

# ---------- Helper: apply saved global theme ----------
apply_global_theme() {
    local backup_dir="$1"
    local theme_file="$backup_dir/global-theme-id.txt"

    if [ ! -f "$theme_file" ]; then
        return
    fi

    local THEME_ID
    THEME_ID="$(head -n1 "$theme_file" | tr -d '\r\n')"

    if [ -z "$THEME_ID" ]; then
        return
    fi

    echo "🎨 Saved global theme in backup: '$THEME_ID'"

    local exists="no"
    if [ -d "$HOME/.local/share/plasma/look-and-feel/$THEME_ID" ] ||        [ -d "/usr/share/plasma/look-and-feel/$THEME_ID" ] ||        [ -f "$HOME/.local/share/plasma/look-and-feel/$THEME_ID.desktop" ] ||        [ -f "/usr/share/plasma/look-and-feel/$THEME_ID.desktop" ]; then
        exists="yes"
    fi

    if [ "$exists" = "no" ]; then
        echo "⚠️ Global theme '$THEME_ID' is not installed on this system."
        echo "   Install it (System Settings → Global Theme → Get New…)"
        read -r -p "After installing it, press ENTER to try applying it, or type 'skip' to continue: " ans

        if [ "$ans" = "skip" ] || [ "$ans" = "s" ]; then
            echo "Skipping global theme application."
            return
        fi

        # Re-check after user had a chance to install it
        if [ ! -d "$HOME/.local/share/plasma/look-and-feel/$THEME_ID" ] &&            [ ! -d "/usr/share/plasma/look-and-feel/$THEME_ID" ] &&            [ ! -f "$HOME/.local/share/plasma/look-and-feel/$THEME_ID.desktop" ] &&            [ ! -f "/usr/share/plasma/look-and-feel/$THEME_ID.desktop" ]; then
            echo "❌ Theme '$THEME_ID' still not found; skipping."
            return
        fi
    fi

    if command -v lookandfeeltool >/dev/null 2>&1; then
        echo "🔄 Applying global theme '$THEME_ID' via lookandfeeltool…"
        lookandfeeltool -a "$THEME_ID" 2>/dev/null ||         lookandfeeltool -a "$THEME_ID.desktop" 2>/dev/null ||         echo "⚠️ Failed to apply theme with lookandfeeltool."
    else
        echo "⚠️ 'lookandfeeltool' not found; cannot auto-apply global theme."
    fi
}

# ---------- Helper: maybe rewrite old $HOME paths inside backup (cross-user/machine) ----------
maybe_rewrite_home_paths() {
    local backup_dir="$1"
    local meta="$backup_dir/meta.txt"

    if [ ! -f "$meta" ]; then
        return
    fi

    local ORIGINAL_HOME ORIGINAL_USER ORIGINAL_HOST
    ORIGINAL_HOME=""
    ORIGINAL_USER=""
    ORIGINAL_HOST=""

    # shellcheck source=/dev/null
    . "$meta"

    if [ -z "$ORIGINAL_HOME" ] || [ "$ORIGINAL_HOME" = "$HOME" ]; then
        return
    fi

    echo "🌍 Detected backup from different user or machine."
    echo "   ORIGINAL_HOME: $ORIGINAL_HOME"
    echo "   CURRENT_HOME:  $HOME"
    echo "   Rewriting paths inside backup to match this user…"

    # Only patch text files; skip binaries
    # -R: recursive, -I: ignore binary files, -l: list matching files only
    mapfile -t files_to_patch < <(grep -RIl "$ORIGINAL_HOME" "$backup_dir" 2>/dev/null || true)

    for f in "${files_to_patch[@]}"; do
        [ -f "$f" ] || continue
        sed -i "s|$ORIGINAL_HOME|$HOME|g" "$f" || true
    done

    echo "✅ Path rewrite complete."
}

# ---------- Helper: ensure backup directory extracted & patched ----------
ensure_backup_ready() {
    local NAME="$1"

    ensure_base_dir
    local BACKUP_DIR="$BASE_DIR/$NAME"

    if [ -f "$BASE_DIR/$NAME.tar.gz" ]; then
        echo "📦 Extracting archive $BASE_DIR/$NAME.tar.gz…"
        tar -xzf "$BASE_DIR/$NAME.tar.gz" -C "$BASE_DIR"
    fi

    if [ ! -d "$BACKUP_DIR" ]; then
        echo "❌ Backup '$NAME' not found in $BASE_DIR"
        exit 1
    fi

    # If backup was created on a different user/machine, rewrite $HOME paths
    maybe_rewrite_home_paths "$BACKUP_DIR"

    echo "$BACKUP_DIR"
}

# ---------- BACKUP ----------
backup() {
    local NAME="$1"
    if [ -z "$NAME" ]; then
        echo "❌ Please provide a backup name."
        echo "Usage: kde-theme backup <name>"
        exit 1
    fi

    ensure_base_dir
    local BACKUP_DIR="$BASE_DIR/$NAME"
    mkdir -p "$BACKUP_DIR"

    echo "🔵 Backing up KDE theme/layout as '$NAME'..."
    echo "Backup directory: $BACKUP_DIR"

    # -1) Metadata (for cross-user/machine restore)
    {
        echo "ORIGINAL_HOME=$HOME"
        echo "ORIGINAL_USER=${USER:-unknown}"
        echo "ORIGINAL_HOST=$(hostname 2>/dev/null || echo unknown-host)"
        echo "CREATED_AT=$(date -Iseconds 2>/dev/null || date)"
        echo "FORMAT_VERSION=1"
    } > "$BACKUP_DIR/meta.txt"

    # 0) Save current LookAndFeelPackage from kdeglobals (global theme ID)
    if [ -f "$HOME/.config/kdeglobals" ]; then
        local THEME_ID
        THEME_ID="$(sed -n 's/^LookAndFeelPackage=//p' "$HOME/.config/kdeglobals" | head -n1 | tr -d '\r\n')"
        if [ -n "$THEME_ID" ]; then
            echo "$THEME_ID" > "$BACKUP_DIR/global-theme-id.txt"
            echo "🎨 Detected current global theme: '$THEME_ID'"
        fi
    fi

    # 1) Layout (panels, widgets, activities)
    if [ -f "$HOME/.config/plasma-org.kde.plasma.desktop-appletsrc" ]; then
        cp "$HOME/.config/plasma-org.kde.plasma.desktop-appletsrc" "$BACKUP_DIR/"
    fi

    # 2) Core KDE/Plasma config files (added kcminputrc for cursor, sounds)
    local cfg_files=(
        plasmarc
        kdeglobals
        kwinrc
        kwinrulesrc
        kscreenlockerrc
        systemsettingsrc
        konsolerc
        dolphinrc
        kcminputrc
    )

    for f in "${cfg_files[@]}"; do
        if [ -f "$HOME/.config/$f" ]; then
            cp "$HOME/.config/$f" "$BACKUP_DIR/"
        fi
    done

    # 3) Plasma & theme assets in ~/.local/share
    if [ -d "$HOME/.local/share" ]; then
        pushd "$HOME/.local/share" >/dev/null 2>&1

        local theme_dirs=(
            plasma
            plasma*
            icons
            color-schemes
            konsole
            aurorae
            wallpapers
            sounds
        )

        for d in "${theme_dirs[@]}"; do
            for real in $d; do
                if [ -e "$real" ]; then
                    cp -r "$real" "$BACKUP_DIR/" 2>/dev/null || true
                fi
            done
        done

        popd >/dev/null 2>&1
    fi

    # 3b) User icon themes (including cursors) in ~/.icons
    if [ -d "$HOME/.icons" ]; then
        cp -r "$HOME/.icons" "$BACKUP_DIR/user-icons" 2>/dev/null || true
    fi

    # 4) Kvantum themes & config
    if [ -d "$HOME/.config/Kvantum" ]; then
        cp -r "$HOME/.config/Kvantum" "$BACKUP_DIR/Kvantum-config"
    fi
    if [ -d "$HOME/.local/share/Kvantum" ]; then
        cp -r "$HOME/.local/share/Kvantum" "$BACKUP_DIR/Kvantum-themes"
    fi

    # 5) Latte Dock config & layouts
    if compgen -G "$HOME/.config/latte*" > /dev/null; then
        mkdir -p "$BACKUP_DIR/latte-config"
        cp -r "$HOME"/.config/latte* "$BACKUP_DIR/latte-config/" 2>/dev/null || true
    fi
    if compgen -G "$HOME/.local/share/latte*" > /dev/null; then
        mkdir -p "$BACKUP_DIR/latte-share"
        cp -r "$HOME"/.local/share/latte* "$BACKUP_DIR/latte-share/" 2>/dev/null || true
    fi

    # 6) Tar snapshot of this backup
    tar -czf "$BASE_DIR/$NAME.tar.gz" -C "$BASE_DIR" "$NAME"

    echo "✅ Backup complete!"
    echo " - Directory: $BACKUP_DIR"
    echo " - Archive:   $BASE_DIR/$NAME.tar.gz"
    echo
    echo "You can copy JUST the .tar.gz to another user/machine running KDE,"
    echo "place it in $BASE_DIR there, and run:"
    echo "  kde-theme restore-all $NAME"
}

# ---------- RESTORE THEME ONLY (no layout overwrite) ----------
restore_theme() {
    local NAME="$1"
    if [ -z "$NAME" ]; then
        echo "❌ Please provide the backup name to restore."
        echo "Usage: kde-theme restore <name>"
        exit 1
    fi

    local BACKUP_DIR
    BACKUP_DIR="$(ensure_backup_ready "$NAME")"

    echo "🟣 Restoring KDE theme (without layout) from '$NAME'..."

    # 1) Core KDE config files
    local cfg_files=(
        plasmarc
        kdeglobals
        kwinrc
        kwinrulesrc
        kscreenlockerrc
        systemsettingsrc
        konsolerc
        dolphinrc
        kcminputrc
    )

    mkdir -p "$HOME/.config"
    for f in "${cfg_files[@]}"; do
        if [ -f "$BACKUP_DIR/$f" ]; then
            cp "$BACKUP_DIR/$f" "$HOME/.config/"
        fi
    done

    # 2) Plasma & theme assets (including sounds)
    mkdir -p "$HOME/.local/share"
    local theme_dirs=(
        plasma
        plasma-desktop
        plasma-shell
        icons
        color-schemes
        konsole
        aurorae
        wallpapers
        sounds
    )

    for d in "${theme_dirs[@]}"; do
        if [ -d "$BACKUP_DIR/$d" ]; then
            cp -r "$BACKUP_DIR/$d" "$HOME/.local/share/"
        fi
    done

    # Any plasma* directories
    for d in "$BACKUP_DIR"/plasma*; do
        [ -d "$d" ] || continue
        cp -r "$d" "$HOME/.local/share/"
    done

    # 2b) User icon themes ~/.icons
    if [ -d "$BACKUP_DIR/user-icons" ]; then
        mkdir -p "$HOME/.icons"
        cp -r "$BACKUP_DIR/user-icons/"* "$HOME/.icons/" 2>/dev/null || true
    fi

    # 3) Kvantum
    if [ -d "$BACKUP_DIR/Kvantum-config" ]; then
        mkdir -p "$HOME/.config"
        rm -rf "$HOME/.config/Kvantum"
        cp -r "$BACKUP_DIR/Kvantum-config" "$HOME/.config/Kvantum"
    fi
    if [ -d "$BACKUP_DIR/Kvantum-themes" ]; then
        mkdir -p "$HOME/.local/share"
        rm -rf "$HOME/.local/share/Kvantum"
        cp -r "$BACKUP_DIR/Kvantum-themes" "$HOME/.local/share/Kvantum"
    fi

    # 4) Latte config/share
    if [ -d "$BACKUP_DIR/latte-config" ]; then
        mkdir -p "$HOME/.config"
        cp -r "$BACKUP_DIR/latte-config/"* "$HOME/.config/" 2>/dev/null || true
    fi
    if [ -d "$BACKUP_DIR/latte-share" ]; then
        mkdir -p "$HOME/.local/share"
        cp -r "$BACKUP_DIR/latte-share/"* "$HOME/.local/share/" 2>/dev/null || true
    fi

    # Apply global theme if saved
    apply_global_theme "$BACKUP_DIR"

    echo "🔄 Reloading KWin configuration…"
    qdbus org.kde.KWin /KWin reconfigure 2>/dev/null || true

    echo "✅ Theme restore complete (layout untouched)."
}

# ---------- RESTORE LAYOUT ----------
restore_layout() {
    local NAME="$1"
    if [ -z "$NAME" ]; then
        echo "❌ Please provide the backup name to restore layout from."
        echo "Usage: kde-theme restore-layout <name>"
        exit 1
    fi

    local BACKUP_DIR
    BACKUP_DIR="$(ensure_backup_ready "$NAME")"

    echo "⚠️  WARNING: This will overwrite your current Plasma panel/widget layout"
    echo "    with the one from backup '$NAME'."
    echo "    Your newly added widgets/panels may be lost."

    check_missing_plasmoids "$BACKUP_DIR"

    if [ ! -f "$BACKUP_DIR/plasma-org.kde.plasma.desktop-appletsrc" ]; then
        echo "❌ No plasma-org.kde.plasma.desktop-appletsrc found in backup; cannot restore layout."
        exit 1
    fi

    mkdir -p "$HOME/.config"
    cp "$BACKUP_DIR/plasma-org.kde.plasma.desktop-appletsrc" "$HOME/.config/"

    # Latte configs/layouts
    if [ -d "$BACKUP_DIR/latte-config" ]; then
        mkdir -p "$HOME/.config"
        cp -r "$BACKUP_DIR/latte-config/"* "$HOME/.config/" 2>/dev/null || true
    fi
    if [ -d "$BACKUP_DIR/latte-share" ]; then
        mkdir -p "$HOME/.local/share"
        cp -r "$BACKUP_DIR/latte-share/"* "$HOME/.local/share/" 2>/dev/null || true
    fi

    echo "🔄 Restarting Plasma shell for layout restore…"
    kquitapp5 plasmashell 2>/dev/null || true
    kstart5 plasmashell 2>/dev/null || true
    qdbus org.kde.KWin /KWin reconfigure 2>/dev/null || true

    echo "✅ Layout restore complete."
}

# ---------- RESTORE BOTH ----------
restore_all() {
    local NAME="$1"
    if [ -z "$NAME" ]; then
        echo "❌ Please provide the backup name."
        echo "Usage: kde-theme restore-all <name>"
        exit 1
    fi

    echo "🟣 Restoring THEME + LAYOUT from '$NAME'..."
    restore_theme "$NAME"
    restore_layout "$NAME"
}

# ---------- LIST ----------
list_backups() {
    ensure_base_dir
    echo "📁 Available KDE theme backups in $BASE_DIR:"
    ls "$BASE_DIR"
}

case "$1" in
    backup)
        backup "$2"
        ;;
    restore)
        restore_theme "$2"
        ;;
    restore-layout)
        restore_layout "$2"
        ;;
    restore-all)
        restore_all "$2"
        ;;
    list)
        list_backups
        ;;
    *)
        echo "KDE theme backup/restore utility"
        echo
        echo "Usage:"
        echo "  kde-theme backup <name>         # create a named backup (theme + layout + Kvantum + Latte + sounds + cursors)"
        echo "  kde-theme restore <name>        # restore THEME ONLY (no widget/panel layout overwrite)"
        echo "  kde-theme restore-layout <name> # restore widget/panel/Latte layout from backup"
        echo "  kde-theme restore-all <name>    # restore theme + layout together"
        echo "  kde-theme list                  # list available backups"
        echo
        echo "Backups are stored in: $BASE_DIR"
        echo "Each backup is also archived as: $BASE_DIR/<name>.tar.gz"
        echo "You can copy the .tar.gz to another KDE user/machine and restore there."
        ;;
esac
